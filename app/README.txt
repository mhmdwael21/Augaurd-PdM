To start the project:
1-create a directory called "app".
2-add project files to it.
3-open terminal.
4-write `pip install -r requirements.txt`.
5-write `python -m uvicorn app.main:app --reload` at the parent directory of `app` directory.